# Copy to .env (gitignored) and fill in real values before running up.sh.

# Required - superuser password for the database.
POSTGRES_PASSWORD=

# Optional - superuser name and default database, both default to "postgres".
#POSTGRES_USER=
#POSTGRES_DB=

# Optional - log level shared by all services, defaults to "info".
#LOG_LEVEL=
